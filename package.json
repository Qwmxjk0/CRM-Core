{
  "name": "crm-core",
  "private": true,
  "devDependencies": {
    "next": "16.1.6"
  },
  "workspaces": [
    "apps/api",
    "packages/shared"
  ],
  "scripts": {
    "build": "npm --workspace apps/api run build",
    "dev": "npm --workspace apps/api run dev",
    "lint": "npm --workspace apps/api run lint",
    "migrate": "npx supabase link --project-ref $SUPABASE_PROJECT_REF --yes && npx supabase db push --yes"
  }
}
