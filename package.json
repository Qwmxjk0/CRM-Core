{
  "name": "crm-core",
  "private": true,
  "workspaces": [
    "apps/api",
    "packages/shared"
  ],
  "scripts": {
    "build": "npm --workspace apps/api run build",
    "dev": "npm --workspace apps/api run dev",
    "lint": "npm --workspace apps/api run lint",
    "migrate": "npx supabase db push --project-ref $SUPABASE_PROJECT_REF"
  }
}
